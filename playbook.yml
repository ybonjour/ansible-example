---
- hosts: greeters
  remote_user: vagrant
  vars:
    name: "Yves"
  tasks:
    - name: Create greeting container
      file:
        path: ~/greeting
        state: touch

    - name: Greet
      shell: echo "Hello {{name}}" > ~/greeting


